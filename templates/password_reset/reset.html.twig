{% extends 'base.html.twig' %}
{% block title %}Password Reset{% endblock %}

{% block body %}
{{ form_start(form) }}
<div class="container">
    <form>
        <div class="form-group">
            <div class="col-xs-2">
                <h1>Reset Password</h1>
                {% if memberEmailNotFound is defined %}
                <div><p>{{ memberEmailNotFound }}</p></div>
                {% endif %}
                {% if requestAlreadyExists is defined %}
                    <div><p>A password reset has already been requested.</p></div>
                {% endif %}
                {{ form_label(form.email, 'Email Address') }}
                {{ form_widget(form.email, {'attr': {'class': 'form-control'} }) }}
                {{ form_row(form._token) }}
            </div>
        </div>

        <div class="form-group"> <!-- Submit button !-->
            <button type="submit" class="btn btn-primary">Send Email</button>
        </div>

    </form>
</div>

{{ form_end(form) }}
{% endblock %}

{#{% block title %}Password Reset{% endblock %}#}

{#{% block body %}#}

    {#{{ include('password_reset/passwordReset.html.twig') }}#}

{#{% endblock %}#}
