
{% extends 'base.html.twig' %}

{% block title %}Show{% endblock %}
{% block body %}
    <h1>Show</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ show.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ show.name }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ show.date ? show.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>TicketPrice</th>
                <td>{{ show.ticketPrice }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ show.location }}</td>
            </tr>
            <tr>
                <th>Synopsis</th>
                <td>{{ show.synopsis }}</td>
            </tr>
            <tr>
                <th>PicturePath</th>
                <td><img class="img-fluid rounded mx-auto d-block shadow-lg p-3 mb-5 bg-white rounded" src="images/{{ asset(show.picturePath) }}"/></td>
            </tr>
            <tr>
                <th>TicketLink</th>
                <td>{{ show.ticketLink }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('show_index') }}">back to list</a>
            <tr>
                <th>Synopsis</th>
                <td>{{ show.synopsis }}</td>
            </tr>

    {#<a href="{{ path('show_index') }}">back to list</a>#}

    {#<a href="{{ path('show_edit', {'id': show.id}) }}">edit</a>#}

    {#{{ include('show/_delete_form.html.twig') }}#}
	{{ include('show/ticketBtn.html.twig', {'show': show} ) }}
{% endblock %}
